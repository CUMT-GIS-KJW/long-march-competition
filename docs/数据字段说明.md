# 数据字段说明

节点核心字段：`id/name/date/type/stage/province/city/lat/lng/importance/description`。路线坐标使用 Leaflet 顺序 `[纬度, 经度]`；转换 GeoJSON 时必须改为 `[经度, 纬度]`。

后续 MySQL 表：`march_events`、`march_route_points`、`red_resources`、`analysis_results`。生产级空间分析建议进一步迁移至 PostgreSQL/PostGIS，并使用 CGCS2000。

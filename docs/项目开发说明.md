# 项目开发说明

系统采用“两页一服务”结构：主地图负责长征路线叙事，GIS 分析页负责测绘分析表达，Node 服务统一托管静态资源并提供 API。

当前为 `static` 数据模式，页面通过 `DataService` 读取 `data`。真实数据库接入后，只需将 `features/shared/frontend/config.js` 的 `dataMode` 改为 `api`。

运行：在项目根目录执行 `npm start`，访问 `http://localhost:8096`。
